-- CREATE TABLE IF NOT EXISTS PRODUCT
-- (id INTEGER PRIMARY KEY UNIQUE NOT null ,
-- `name` varchar(200),
-- brand varchar(300));

CREATE TABLE IF NOT EXISTS product ( id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, brand VARCHAR(100) NOT NULL, `name` VARCHAR(100) NOT NULL);
